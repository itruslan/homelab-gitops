apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: proxmox-lb
  namespace: argocd
spec:
  project: infra
  source:
    repoURL: https://github.com/itruslan/homelab-gitops.git
    targetRevision: HEAD
    path: apps/proxmox-lb
  destination:
    server: https://kubernetes.default.svc
    namespace: proxmox-lb
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
