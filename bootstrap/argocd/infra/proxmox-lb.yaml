apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: proxmox-lb
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
  - clusters:
      selector:
        matchExpressions:
        - key: deploy_proxmox_lb
          operator: In
          values: ['true']
  template:
    metadata:
      name: '{{.name}}-proxmox-lb'
    spec:
      project: infra
      sources:
      - repoURL: https://github.com/itruslan/homelab-gitops.git
        targetRevision: HEAD
        path: clusters/{{.name}}/proxmox-lb/manifests
        directory:
          recurse: true
      destination:
        name: '{{.name}}'
        namespace: proxmox-lb
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
        - CreateNamespace=true
