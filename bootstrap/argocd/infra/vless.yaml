apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: vless
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
  - clusters:
      selector:
        matchExpressions:
        - key: deploy_vless
          operator: In
          values: ['true']
  template:
    metadata:
      name: '{{.name}}-vless'
    spec:
      project: infra
      sources:
      - repoURL: https://github.com/itruslan/homelab-gitops.git
        targetRevision: HEAD
        path: charts/infra/vless
        helm:
          releaseName: vless
          ignoreMissingValueFiles: true
          valueFiles:
          - values.yaml
          - ../../../clusters/{{.name}}/vless/values.yaml
      - repoURL: https://github.com/itruslan/homelab-gitops.git
        targetRevision: HEAD
        path: clusters/{{.name}}/vless/manifests
        directory:
          recurse: true
      destination:
        name: '{{.name}}'
        namespace: vless
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
        - CreateNamespace=true
