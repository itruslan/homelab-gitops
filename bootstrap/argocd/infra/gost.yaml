apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: gost
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
  - clusters:
      selector:
        matchExpressions:
        - key: deploy_gost
          operator: In
          values: ['true']
  template:
    metadata:
      name: '{{.name}}-gost'
    spec:
      project: infra
      sources:
      - repoURL: https://github.com/itruslan/homelab-gitops.git
        targetRevision: HEAD
        path: clusters/{{.name}}/gost/manifests
        directory:
          recurse: true
      destination:
        name: '{{.name}}'
        namespace: gost
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
        - CreateNamespace=true
