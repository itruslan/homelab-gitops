apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: cert-sync
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
  - clusters:
      selector:
        matchExpressions:
        - key: deploy_cert_sync
          operator: In
          values: ['true']
  template:
    metadata:
      name: '{{.name}}-cert-sync'
    spec:
      project: infra
      sources:
      - repoURL: https://github.com/itruslan/homelab-gitops.git
        targetRevision: HEAD
        path: clusters/{{.name}}/cert-sync/manifests
        directory:
          recurse: true
      destination:
        name: '{{.name}}'
        namespace: cert-sync
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
        - CreateNamespace=true
