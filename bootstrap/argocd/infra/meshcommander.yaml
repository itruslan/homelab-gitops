apiVersion: argoproj.io/v1alpha1
kind: ApplicationSet
metadata:
  name: meshcommander
  namespace: argocd
spec:
  goTemplate: true
  goTemplateOptions: ["missingkey=error"]
  generators:
  - clusters:
      selector:
        matchExpressions:
        - key: deploy_meshcommander
          operator: In
          values: ['true']
  template:
    metadata:
      name: '{{.name}}-meshcommander'
    spec:
      project: infra
      sources:
      - repoURL: https://github.com/itruslan/homelab-gitops.git
        targetRevision: HEAD
        path: clusters/{{.name}}/meshcommander/manifests
        directory:
          recurse: true
      destination:
        name: '{{.name}}'
        namespace: meshcommander
      syncPolicy:
        automated:
          prune: true
          selfHeal: true
        syncOptions:
        - CreateNamespace=true
