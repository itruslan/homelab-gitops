fullnameOverride: cloudflared-tunnel

cloudflare:
  existingSecret:
    enabled: true
    name: cloudflared-tunnel
    key: tunnelToken

replicaCount: 2

resources:
  requests:
    cpu: 50m
    memory: 64Mi
  limits:
    cpu: "1"
    memory: 256Mi
