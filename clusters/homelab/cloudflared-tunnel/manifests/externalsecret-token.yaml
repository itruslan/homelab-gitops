# Vault KV v2: homelab/teleport/cloudflare → ключ tunnel_token (токен Remote-managed tunnel из Zero Trust).
# Имя Secret и ключ совпадают с чартом cloudflare-tunnel-remote (tunnelToken → TUNNEL_TOKEN).
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: cloudflared-tunnel-token
  namespace: cloudflared-tunnel
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
spec:
  secretStoreRef:
    kind: ClusterSecretStore
    name: vault
  refreshInterval: 1h
  target:
    name: cloudflared-tunnel
    creationPolicy: Owner
  data:
    - secretKey: tunnelToken
      remoteRef:
        key: homelab/teleport/cloudflare
        property: tunnel_token
