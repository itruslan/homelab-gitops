apiVersion: resources.teleport.dev/v2
kind: TeleportProvisionToken
metadata:
  name: teleport-proxy-app
  namespace: teleport
  annotations:
    argocd.argoproj.io/sync-wave: "-3"
spec:
  join_method: kubernetes
  roles:
    - Proxy
    - App
  kubernetes:
    allow:
      - service_account: teleport:teleport-proxy
