apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: gost-tls
  namespace: gost
spec:
  secretName: gost-tls
  issuerRef:
    name: letsencrypt
    kind: ClusterIssuer
  dnsNames:
  - hshp-vps.itruslan.ru
