external-dns:
  provider:
    name: cloudflare
  env:
  - name: CF_API_TOKEN
    valueFrom:
      secretKeyRef:
        name: cloudflare-token
        key: token
  domainFilters:
  - itruslan.ru
  policy: sync
  txtOwnerId: hshp-vps
  sources:
  - ingress
  - service
