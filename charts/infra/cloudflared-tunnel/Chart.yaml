apiVersion: v2
name: cloudflare-tunnel-remote
description: >
  Remotely managed Cloudflare Tunnel. Fork of cloudflare/helm-charts chart v0.1.2 with
  optional existingSecret for External Secrets/Vault (upstream only supports tunnel_token in values).
type: application
version: 0.1.3
appVersion: "latest"
icon: https://developers.cloudflare.com/cloudflare-one/favicon-32x32.png
