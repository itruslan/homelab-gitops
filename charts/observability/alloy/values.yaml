alloy:
  alloy:
    configMap:
      # Переопределяется в {cluster}/values.yaml — задай zone и targets под каждый кластер
      content: ""

  controller:
    type: deployment
    replicas: 1
    podAnnotations:
      configmap.reloader.stakater.com/reload: "alloy"

  rbac:
    create: true

  serviceAccount:
    create: true
